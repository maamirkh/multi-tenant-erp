---
id: 0008
title: Epic 10 Installments tasks correction pass
stage: tasks
date: 2026-08-24
surface: agent
model: claude-sonnet-5
feature: 010-installments
branch: 010-installments
user: Muhammad Amir
command: /sp.tasks
labels: [installments, epic-10, tasks, correction, idempotency, accounting-integration, dependency-graph, parallel-safety]
links:
  spec: specs/010-installments/spec.md
  ticket: null
  adr: null
  pr: null
files:
 - specs/010-installments/tasks.md
tests:
 - none (task-correction-only phase; no code, migrations, or tests implemented per stop condition)
---

## Prompt

Perform a TARGETED CORRECTION PASS on `specs/010-installments/tasks.md`. This is NOT a task-regeneration request. The current tasks.md is structurally strong and should be preserved as much as possible. Correct ONLY the defects listed below and any task numbering/dependency/count/traceability consequences directly caused by those corrections.

AUTHORITATIVE SOURCES: spec.md, plan.md, current repository implementation, current tasks.md. IMPORTANT: DO NOT modify spec.md. DO NOT modify plan.md. DO NOT redesign Epic 10. DO NOT weaken any existing security, accounting, atomicity, concurrency, tenant-isolation, RBAC, entitlement, migration, or regression requirement. Preserve existing task descriptions and numbering wherever practical. If adding/moving/splitting tasks changes task IDs, update ALL downstream Depends-on references, phase counts, total counts, [P] count, traceability tables, scenario mappings, BR/FR mappings, and final report accordingly. Do not claim READY FOR /sp.implement until every correction below is resolved and the final dependency graph is internally valid.

**CORRECTION 1 — INSTALLMENT CONFIGURATION UNIQUENESS**: T001's `installment_configurations` uses `UNIQUE(company_id, branch_id)`, insufficient because PostgreSQL allows multiple NULLs under a normal unique constraint. Correct to use two explicit partial unique indexes: `unique (company_id, branch_id) WHERE branch_id IS NOT NULL` for branch-specific configuration, and `unique (company_id) WHERE branch_id IS NULL` for the company-level/default configuration. Ensure corresponding model/repository tasks and tests reflect this invariant. Add or amend a test proving: only one company-level configuration may exist per company; different branch overrides may coexist; duplicate configuration for the same branch is rejected. Do not change the approved configuration semantics.

**CORRECTION 2 — COMPLETE HIGH-RISK IDEMPOTENCY GRAPH**: Phase 5.5 is the HARD PREREQUISITE for all high-risk/idempotency-protected commands: activation, collection, settlement execution, collection reversal, rescheduling, cancellation, default, write-off. Every applicable command must have an explicit, executable dependency on the Phase 5.5 idempotency primitive and follow the approved idempotency transaction semantics. Named inconsistencies: reversal implementation doesn't fully expose/wire idempotency; cancellation lacks the required T086/idempotency dependency; write-off lacks it too; default is implemented before Phase 5.5 despite the hard-gate declaration. IMPORTANT FOR DEFAULT: do not remove the Phase 5 lifecycle transition definition if useful for the legal state machine — instead distinguish (A) the lifecycle/domain transition primitive, which may stay in Phase 5, from (B) the externally callable/high-risk idempotent default command/orchestration, which MUST NOT be implementable before Phase 5.5. The final graph must make it impossible for /sp.implement to implement a public high-risk default command before the idempotency primitive exists. For every high-risk command ensure the task explicitly defines: reserve idempotency key → acquire required row lock → validate operation → stage business/Installments/Accounting changes → stage audit/outbox/idempotency completion → Accounting finalize/commit owner LAST — and that rollback does not strand an IN_PROGRESS idempotency row. Endpoint tasks saying "idempotency required" are NOT sufficient by themselves; service/orchestration tasks must explicitly implement the mechanism.

**CORRECTION 3 — PHASE 3 ACCOUNTING READ BOUNDARY**: T046/T048 require Accounting AR/outstanding information before the Phase 6 AccountingIntegrationGateway task exists. The architecture requires a SINGLE controlled Installments→Accounting module boundary and must not encourage Phase 3 code to directly import Accounting models/repositories. Preferred structure: introduce an early/read-only Accounting integration gateway/interface/skeleton before T046/T048, exposing only the live-read operations eligibility/draft calculation needs; prohibit direct imports of modules.accounting.models and modules.accounting.repositories; Phase 6 EXTENDS the same gateway with staged/finalize money-mutating methods. Do NOT create two competing Accounting integration abstractions. Do NOT duplicate Accounting balances inside Installments. Ensure T046/T048 depend on the actual read-boundary task.

**CORRECTION 4 — PHASE 6 FAILURE-INJECTION TEST ORDER**: T114-T116 reference Installments-side entities/workflows created only in later phases (InstallmentAllocationReference, InstallmentLateCharge, complete write-off orchestration). A test must never depend on a model/service not yet created. Split the proof into two layers: LAYER A (Phase 6, Accounting-only) — keep release-critical tests proving the new staged Accounting APIs don't prematurely commit (JournalEntry, ARTransaction, Payment, PaymentAllocationLine, CustomerLedger). LAYER B (cross-module) — place/add the full Installments+Accounting failure-injection tests in the earliest phases where the relevant Installments models/services actually exist (collection atomicity after InstallmentAllocationReference exists; late-charge atomicity after InstallmentLateCharge exists; write-off atomicity after full write-off orchestration exists). These must still prove all-or-nothing rollback across Accounting rows/state, Installments rows/state, audit, outbox, idempotency completion. Update phase exit gates and downstream dependencies accordingly. Phase 6 must remain a HARD GATE before Phases 7–10 use AccountingIntegrationGateway mutation methods.

**CORRECTION 5 — T048 SEQUENCE DEPENDENCY**: T048 uses T028's sequence-generation logic but its dependency points only at the underlying sequence model/task. Correct T048 to explicitly depend on the task that IMPLEMENTS the locked sequence/number generation behavior (T028), not merely the model/table. Audit nearby tasks for the same class of dependency error: a task that CALLS a service/repository method must depend on the task that IMPLEMENTS that method, not merely the model/table it uses. Only correct actual errors found.

**CORRECTION 6 — INVALID DEPENDENCIES AND [P] AUDIT**: Fix T253's invalid/self-referential "Depends on: T253 prerequisites" — replace with explicit real task IDs representing the completed changes/phases that must exist before the Sales/CRM/Platform Admin/Users-Roles regression suite runs. Then perform a COMPLETE mechanical audit of every [P] marker. A task may be [P] ONLY if it can actually execute in parallel without: modifying the same file as another unfinished parallel task; depending on another task in that parallel batch; requiring an unfinished API/service; changing the same transaction contract; creating an ordering-sensitive migration; creating conflicting edits to a shared constants/router/registry file. Named example: T013, T014, T015 all modify constants.py and must NOT all be presented as safely parallel. Similarly audit constants.py, router.py, __init__.py, api/v1/router.py, users_roles/constants.py, shared frontend API/hook/index files, migration chain files, any other shared registry. Remove [P] where unsafe. Recalculate the REAL [P] count.

**GLOBAL DEPENDENCY VALIDATION** (14 checks): every Depends-on ID exists; no self-dependency; no circular dependency; no task consumes an artifact before its creator task; no Phase 7–10 Accounting mutation starts before the Phase 6 gate; no high-risk idempotency-protected command executable before Phase 5.5; migration chain strictly linear; frontend tasks depend on actual backend endpoints; E2E tests depend on the workflows they test; cross-module regression runs only after relevant module changes complete; all [P] tasks satisfy the parallel-execution rule; no Accounting model/repository directly imported from Installments business logic; no duplicate Accounting truth/balance introduced into Installments; all failure-injection tests scheduled only after every entity/service they reference exists.

**TRACEABILITY PRESERVATION**: re-verify every FR-INST group, every BR-INST rule individually, all acceptance scenarios A–L, all security/tenant-isolation/RBAC requirements, entitlement-disabled servicing behavior, Platform Admin boundary, SupportAccessGrant boundary, Accounting atomicity, idempotency, concurrency, migrations/backfill, frontend, E2E/regression — no orphan requirement after tasks are moved, split, added, or renumbered.

**FINAL REPORT REQUIRED** (23 items): defects found; exact corrections made; old/new task IDs affected; whether tasks were added/removed/split/moved/renumbered; final total task count; final task count per phase; final [P] count after the audit; Phase 5.5/Phase 6 hard-gate status; all eight high-risk commands and the task proving each is idempotency-protected; Accounting read-boundary task used by early eligibility/draft creation; Phase-6-only vs. later cross-module failure-injection test IDs; corrected T048/T253 dependencies; migration range and current-head assumption; FR-INST/BR-INST/Scenario A–L coverage status; dependency-cycle/orphan validation result; unresolved blockers; final verdict (READY / NOT READY FOR /sp.implement). Do NOT report READY unless all six correction areas and the global dependency audit pass. Make the smallest safe correction set. Preserve the existing approved architecture and task structure wherever possible.

## Response snapshot

Read the full 261-line-equivalent (256-task) `tasks.md` in two passes, then applied all six corrections as targeted `Edit` calls against the OLD task numbers (deliberately deferring renumbering to the very end, to keep content edits and mechanical renumbering as two separable, independently-verifiable steps):

**Correction 1**: Rewrote T001's `installment_configurations` constraint into two explicit partial unique indexes (`uq_installment_configurations_branch ... WHERE branch_id IS NOT NULL`, `uq_installment_configurations_company_default ON (company_id) WHERE branch_id IS NULL`); updated the model task's `__table_args__` description; amended the existing configuration-repository test task (rather than adding a new one) to prove all three required properties, moving it to a real-Postgres dependency since partial unique indexes aren't SQLite-provable.

**Correction 2**: Added explicit idempotency-reservation-first wiring (with `Depends on: T086` and `FOR UPDATE` lock ordering) to the reversal, settlement-execution, reschedule, cancellation, and write-off service-method tasks — none of which had it explicitly, despite their endpoints claiming "idempotency required." For "default," re-scoped the existing Phase 5 `mark_defaulted()` task to be explicitly the *internal, non-idempotent, non-HTTP-reachable* domain-transition primitive only; removed the Phase 5 "Endpoint: Default" task entirely; and inserted two new Phase 10 tasks — a `default_command()` orchestration wrapper (idempotency reservation → lock → calls the Phase 5 primitive → audit/outbox/idempotency-completion staged → single commit) that structurally cannot exist before Phase 5.5, and the moved "Endpoint: Default" task now wired to call the orchestration wrapper instead of the primitive directly.

**Correction 3**: Inserted a new Phase 3 task — a read-only `AccountingIntegrationGateway` skeleton (`get_invoice_outstanding_amount()`, `get_ar_transaction()`, importing only `modules.accounting.services`, never `.models`/`.repositories`) — positioned before the eligibility/draft-creation tasks, which were rewired to depend on it instead of vaguely-worded "Accounting AR read." Updated the Phase 6 gateway task to explicitly *extend* this same class (not create a competing one), and updated its own dependency list accordingly.

**Correction 4**: Rewrote the three Phase 6 forced-failure tests to be strictly Layer-A (Accounting-only: JournalEntry/ARTransaction/CustomerLedger, no reference to InstallmentLateCharge/InstallmentAllocationReference/contract-WRITTEN_OFF-status) and relocated their file paths under `tests/integration/repositories/accounting/` to reflect the narrower scope. Inserted three new Layer-B cross-module forced-failure tests at the earliest point each referenced Installments entity actually exists: after `InstallmentAllocationReference`+the full collection orchestration (Phase 7), after `InstallmentLateCharge`+`apply_late_charge()` (Phase 8), and after the full write-off orchestration (Phase 10). Updated the Phase 6 exit gate to describe the Layer A/Layer B split explicitly and confirm Layer B's absence does not block the gate.

**Correction 5**: Corrected T048's dependency from the sequence *model* task to the task that actually *implements* the locked `generate_next_number()` method; audited the rest of the graph for the same defect class and found no other instances.

**Correction 6**: Replaced the self-referential "Depends on: T253 prerequisites" with five explicit real task IDs (the migration and constants.py/module_enablement.py/capability_seed_service.py edits that touch sibling-module-owned files). Then performed a full mechanical `[P]`-audit, finding and fixing 15 additional same-file violations beyond the named `constants.py` example: `T014`/`T015` (same file as `T013`); `T057`(`_add_months`)/`T059`(residual logic, same file as `T057`'s `ScheduleEngine.generate()`); `T054`/`T055` (same test file); three tasks in the Phase 5.5 idempotency-concurrency test file; three tasks in the Phase 9 completion-guard test file; three tasks in the Phase 10 reschedule test file; two tasks in the Phase 10 cure test file; five tasks in a newly-named shared Phase 11 entitlement-continuity test file; and one mis-marked `router.py`-writing admin-endpoint task (`T042`). Also gave six previously-ambiguous (no explicit filename) test/E2E tasks explicit, distinct filenames to remove ambiguity rather than force them sequential, preserving genuine parallelism where the concern was ambiguity, not an actual conflict (five Phase-14 concurrency-hardening tests, thirteen Phase-15 Playwright specs, two Phase-8 accounting-integration tests, two Phase-11 security tests).

Net structural change: 6 new tasks inserted (Phase 3 Accounting read-boundary gateway; Phase 7/8/10 Layer-B atomicity tests; Phase 10 default-command orchestration + its moved endpoint), 1 task removed (the premature Phase 5 default endpoint) — requiring every task from the old `T046` onward to be renumbered. Performed this renumbering with a Python script (not by hand) using a precisely-derived old→new mapping function accounting for both the insertions and the removal, applied as a single regex pass over the whole document (to avoid double-shifting already-transformed numbers), followed by placeholder-token substitution for the newly-inserted tasks' final IDs. Caught and fixed one arithmetic error in the manually-derived placeholder target numbers (an off-by-one that produced two duplicate IDs, `T137` and `T153`) via direct post-script verification rather than trusting the hand calculation, and one cosmetic self-reference (an already-renumbered quotation of the OLD broken T253 text, inside my own corrective annotation, that the mechanical pass had — correctly, if confusingly — also renumbered).

Ran a full Python-based mechanical validation after every corrective step: confirmed exactly 261 unique, contiguous task IDs (`T001`–`T261`), zero non-existent dependency references, zero self-references, and zero forward references (a task depending on a numerically-later one) — the last of these constitutes a formal proof of acyclicity, since a graph where every edge points to a strictly smaller ID cannot contain a cycle. Recomputed final phase counts (summing to 261) and the final `[P]` count (124, down from the original draft's 139, reflecting the parallel-safety audit's net removals after also accounting for newly-added parallel-safe tasks). Updated the document header to summarize the correction pass and its renumbering; rewrote the "Parallel Execution Guidance" section's example lists using directly-grepped (not hand-computed) post-renumbering task numbers, after catching that my first attempt at rewriting this section itself contained hand-computation errors; added traceability-table entries for all 6 new tasks (`T046`, `T136`, `T152`, `T166`, `T167`, `T184`) across the FR-INST, BR-INST, and Failure/Recovery coverage tables.

## Outcome

- ✅ Impact: All six named defect classes are corrected with a mechanically-verified, cycle-free, fully-contiguous 261-task dependency graph — the plan-to-tasks handoff is now safe for `/sp.implement` to execute without silently violating the Phase 5.5/Phase 6 hard gates or reintroducing the PostgreSQL NULL-uniqueness bug, the read-boundary ambiguity, or any of the 15 additional same-file parallel-marking errors found during the audit.
- 🧪 Tests: none implemented (task-correction-only phase, per explicit stop condition) — the task graph itself now contains more precisely-scoped tests than before (Layer A/B atomicity split, three new uniqueness-invariant assertions folded into an existing task, six ambiguity-removing explicit filenames).
- 📁 Files: `specs/010-installments/tasks.md` updated in place (no other spec files touched, per instruction); this PHR.
- 🔁 Next prompts: `/sp.implement`, starting with Phase 0's migration block, then Phase 1 → ... → Phase 5.5 → Phase 6 (the two hard gates) → Phases 7-15 in order.
- 🧠 Reflection: The off-by-one error in my own hand-derived placeholder ID calculation — caught only because I ran a mechanical duplicate/gap check immediately after the script rather than trusting the arithmetic — is itself a small demonstration of exactly the discipline this whole correction pass was about: verify mechanically, don't trust a plausible-looking manual derivation, especially under a self-imposed constraint (preserve existing numbering wherever practical) that makes errors easy to rationalize away as "close enough."

## Evaluation notes (flywheel)

- Failure modes observed: one self-caught arithmetic error in manual placeholder-ID pre-computation (duplicate `T137`/`T153`), corrected via mechanical validation before it could propagate into the final deliverable. No user-facing failure occurred.
- Graders run and results (PASS/FAIL): N/A — task-correction-only phase, no automated grading harness invoked. A custom Python validation script (dependency-graph integrity: existence, self-reference, forward-reference, duplicate-ID, contiguity) was run three times across the correction pass and is the closest analog to a grader here — all three runs are reported inline above.
- Prompt variant (if applicable): N/A
- Next experiment (smallest change to try): For any future large-scale task-ID renumbering, compute the full old→new mapping programmatically from the insertion/removal event list from the start (rather than partly by hand) — this would have caught the off-by-one before it was ever written to the file, rather than requiring a post-hoc mechanical catch.
